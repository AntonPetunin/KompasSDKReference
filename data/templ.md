<!-- # **Шаблон документации интерфейса КОМПАС API** -->

# **Интерфейс [ИмяИнтерфейса]**

## Иерархия наследования

<style>
.top-parent{
    color: #ffe3d8;
    background: green;
    border: 3px solid #046709;
    border-radius: 4px;
    padding: 5px;
    text-align: center;
    margin-bottom: 5px;
    font-weight: bold;
    width: 40%;
    max-width: 220px;
}

.other-parents{
    color: #ffe3d8;
    background: #4a6fa5;
    border: 3px solid #3d5b87;
    border-radius: 4px;
    padding: 5px;
    text-align: center;
    margin: 5px 10%;
    font-weight: bold;
    width: 40%;
    max-width: 220px;
}

.interface{
    color: #ffe3d8;
    background: #103874;
    border: 3px solid #002152;
    border-radius: 4px;
    padding: 5px;
    text-align: center;
    margin: 5px 10%;
    font-weight: bold;
    width: 40%;
    max-width: 220px;
}
</style>

<div style="padding: 10px; background: #f5f5f5; border-radius: 5px; max-width: auto; margin-bottom: 20px">
<div class="top-parent">IAPIObject</div>

<div style="text-align: left; color:black; margin: 5px 15%;">▼</div>
<div class="other-parents">IKompasAPIObject</div>

<!-- Добавь дополнительные уровни иерархии по мере необходимости -->
<div style="text-align: left; color:black; margin: 5px 25%;">▼</div>
<div class="other-parents" style="margin: 5px 20%;">[РодительскийИнтерфейс]</div>

<div style="text-align: left; color:black; margin: 5px 35%;">▼</div>
<div class="interface" style="margin: 5px 30%;">[ИмяИнтерфейса]</div>
</div>

## Общее описание

[Краткое описание назначения и функциональности интерфейса. Объясни, для чего используется интерфейс, какие задачи решает, в каких сценариях применяется.]

## Важные примечания

1. [Важное примечание 1 - например, про необходимость вызова Update()]
2. [Важное примечание 2 - например, про особенности работы с указателями]
3. [Важное примечание 3 - например, про ограничения или специфику использования]

## Получение интерфейса

### Основные способы получения:

1. **Из коллекции объектов:**
   - `[ИмяКоллекции]::Add` - создание нового объекта
   - `[ИмяКоллекции]::Get[ИмяОбъекта]` - получение по индексу
   - `[ИмяКоллекции]::Get[ИмяОбъекта]ByName` - получение по имени

2. **Из документа или контекста:**
   - `[ИмяДокумента]::Get[ИмяОбъекта]` - получение из документа
   - `[ИмяМенеджера]::Get[ИмяОбъекта]` - получение через менеджер

3. **Приведение типов:**
   - Через `SmartPtr<Interface>` от базовых интерфейсов
   - Автоматическое приведение при наследовании

### Примеры получения:

```cpp
// Пример 1: Получение из коллекции
ksapi::I[ИмяИнтерфейса]Ptr obj = collection->Add();

// Пример 2: Получение из документа
ksapi::IKompasDocument3DPtr doc = application->GetActiveDocument();
ksapi::I[ИмяИнтерфейса]Ptr obj = doc->Get[ИмяИнтерфейса]();
```

### Обязательные функции библиотеки

Для любой библиотеки, работающей с KsAPI, необходимо объявить функции точки входа и выхода:

```cpp
// Точка входа - вызывается при загрузке библиотеки в КОМПАС
APP_EXP_FUNC(bool) LoadKompasLibrary(ksapi::IApplication & app, ksapi::IKompasLibraryActions & libaryActions)
{
    // Здесь сохраняется указатель на приложение КОМПАС
    // Через app доступны все интерфейсы API
    return true;
}

// Точка выхода - вызывается при выгрузке библиотеки
APP_EXP_FUNC(void) UnloadKompasLibrary()
{
    // Освобождение ресурсов
}
```

**Важно:** Функция `LoadKompasLibrary` обязательна и является основным способом получения интерфейса [`IApplication`](files/IApplication.md). Через параметр `app` передаётся экземпляр приложения КОМПАС, к которому подключена библиотека.

## Дополнительные интерфейсы

[Список интерфейсов, которые можно получить через QueryInterface от данного интерфейса. Добавляй ссылки на страницы интерфейсов даже если они ещё не существуют - они появятся позже.]

- [`IUserDataStorage`](files/IUserDataStorage.md) - интерфейс хранения пользовательских данных
- [`IPropertyKeeper`](files/IPropertyKeeper.md) - интерфейс работы со свойствами
- [`[ДругойИнтерфейс]`](files/.md) - [описание назначения]

## Методы интерфейса

[Логически сгруппируй методы по функциональности. Пример группировки:]

### Группа 1: Основные операции

- [`Метод1()`](#метод1) - [краткое описание]
- [`Метод2()`](#метод2) - [краткое описание]

### Группа 2: Параметры и свойства

- [`Метод3()`](#метод3) - [краткое описание]
- [`Метод4()`](#метод4) - [краткое описание]

### Группа 3: Специальные возможности

- [`Метод5()`](#метод5) - [краткое описание]
- [`Метод6()`](#метод6) - [краткое описание]

---

### [ИмяМетода]

[Группа методов](#группа-1-основные-операции) | [К оглавлению](#методы-интерфейса)

**Кратко:** [Одно-два предложения о назначении метода]

**Полное описание:**
[Подробное описание метода, его назначения, особенностей работы. Объясни контекст использования, что делает метод, какие проблемы решает.]

**Синтаксис:**

```cpp
[Полная сигнатура метода из заголовочного файла]
```

**Параметры:**

- `param1` (in/out) - [описание параметра 1, его назначение и допустимые значения]
- `param2` (in/out) - [описание параметра 2, его назначение и допустимые значения]
- `param3` (in) - [описание параметра 3]

**Возвращаемое значение:** [Описание возвращаемого значения. Что означает true/false или конкретные значения]

**Примечание об enum:** Если используется enum с <= 7 константами, перечисли их прямо в описании. Если больше - сделай ссылку на файл с enum в папке `files/`.

#### **Пример использования**

**Минимальный пример:**

```cpp
// [Краткий комментарий что делает]
ksapi::I[Интерфейс]Ptr obj = // получение интерфейса I[Интерфейс]
obj->Метод();
```

**Расширенный пример:**

```cpp
// [Практический пример с комментариями]
ksapi::I[Интерфейс]Ptr obj = // получение интерфейса I[Интерфейс]
if (obj)
{
    obj->Метод(параметры);
}
```

**Примечания:**

- [Важное примечание 1 - особенности работы, ограничения]
- [Важное примечание 2 - требования к вызову других методов]
- [Важное примечание 3 - совместимость с другими методами]

---

[Повтори блок для каждого метода интерфейса]

---

## Частые ошибки

### 1. **[Название ошибки]**

```cpp
// НЕПРАВИЛЬНО
[код с ошибкой]

// ПРАВИЛЬНО
[исправленный код с пояснением]
```

### 2. **[Название ошибки]**

```cpp
// НЕПРАВИЛЬНО
[код с ошибкой]

// ПРАВИЛЬНО
[исправленный код с пояснением]
```

### 3. **[Название ошибки]**

```cpp
// НЕПРАВИЛЬНО
[код с ошибкой]

// ПРАВИЛЬНО
[исправленный код с пояснением]
```

---

## Практические примеры из исходников

### Пример 1: [Название примера]

```cpp
[Код из реального проекта с комментариями]
// Комментарий к важной части кода
```

### Пример 2: [Название примера]

```cpp
[Код из реального проекта с комментариями]
// Комментарий к важной части кода
```

### Пример 3: [Название примера]

```cpp
[Код из реального проекта с комментариями]
// Комментарий к важной части кода
```

---

## Шаблоны использования

### Шаблон 1: [Название шаблона]

```cpp
// Описание когда использовать этот шаблон
[Универсальный шаблон кода]
```

### Шаблон 2: [Название шаблона]

```cpp
// Описание когда использовать этот шаблон
[Универсальный шаблон кода]
```

### Шаблон 3: [Название шаблона]

```cpp
// Описание когда использовать этот шаблон
[Универсальный шаблон кода]
```

---

## Связанные интерфейсы

### Работа в паре с:

- **`[СвязанныйИнтерфейс1]`** - [описание связи]
- **`[СвязанныйИнтерфейс2]`** - [описание связи]

### Часто используется вместе с:

- **`[ДополнительныйИнтерфейс1]`** - [для чего используется]
- **`[ДополнительныйИнтерфейс2]`** - [для чего используется]

---

## **Инструкция по заполнению шаблона:**

### 1. **Заголовок и иерархия**

- Замени `[ИмяИнтерфейса]` на реальное имя интерфейса
- Настрой иерархию наследования в CSS-блоках
- Укажи правильные родительские интерфейсы

### 2. **Общее описание**

- Кратко: что делает интерфейс?
- Контекст: где используется?
- Назначение: какие задачи решает?

### 3. **Важные примечания**

- Критические моменты (Update(), проверка указателей и т.д.)
- Ограничения и особенности
- Требования к использованию

### 4. **Методы интерфейса**

#### Для каждого метода:

- **Кратко** - 1-2 предложения
- **Полное описание** - подробно, с контекстом
- **Синтаксис** - точная сигнатура из .h файла
- **Параметры** - с указанием направления (in/out). Не пиши "нет параметров"
- **Возвращаемое значение** - описание. Не пиши "нет", если возвращать нечего
- **Примеры** - минимальный и расширенный с комментариями
- **Примечания** - важные детали

### 5. **Примеры кода**

- Используй реальные примеры из проектов
- Добавляй комментарии к сложным моментам
- Показывай типичные сценарии использования
- Для указателей интерфейсов используй формат:
  ```cpp
  ksapi::I[Интерфейс]Ptr obj = // получение интерфейса I[Интерфейс]
  ```
  Вместо:
  ```cpp
  ksapi::I[Интерфейс]Ptr obj = nullptr;
  ```

### 6. **Частые ошибки**

- Конкретные ошибки с исправлениями
- Антипаттерны и как их избежать
- Распространенные заблуждения

### 7. **Важные правила**

- **Не используй номера строк** при ссылках на исходные файлы (они могут измениться)
- **Только русский и английский языки** - не используй китайский, польский или любые другие символы/слова
- **Добавляй ссылки на интерфейсы** - даже если страница не существует, ссылка должна быть (файл появится позже)
- **Enum до 7 констант** - включай прямо в описание метода
- **Большие enum** - ссылка на файл в папке `files/`

---

## **Пример заполнения для конкретного интерфейса:**

Если передашь мне:

1. Имя интерфейса (например, `ISketch`)
2. Заголовочный файл с методами
3. Примеры использования из твоих проектов
4. Контекст (где используется, для чего)
