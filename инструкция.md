# Инструкция для генерации документации КОМПАС API

## Быстрый старт (одна команда)

**Вариант 1: Один запрос**

```
СОЗДАЙ ДОКУМЕНТАЦИЮ ДЛЯ ИНТЕРФЕЙСА [ИмяИнтерфейса]

ИСПОЛЬЗУЯ:
1. ШАБЛОН: templ.md
2. ОПРЕДЕЛЕНИЕ ИНТЕРФЕЙСА: найди в KsAPI/KsAPI.h
3. ПРИМЕРЫ: найди в папке Source/
4. ДОПОЛНИТЕЛЬНЫЕ ИНТЕРФЕЙСЫ: найди в KsAPI/KsAPI.h
```

**Вариант 2: Агент следует инструкции сам**

Перед началом работы дай агенту эту инструкцию:

```
ПРОЧТИ ИНСТРУКЦИЮ ИЗ ФАЙЛА инструкция.md И СОЗДАЙ ДОКУМЕНТАЦИЮ ДЛЯ ИНТЕРФЕЙСА [ИмяИнтерфейса]

СЛЕДУЙ ПОШАГОВОЙ ИНСТРУКЦИИ ИЗ РАЗДЕЛА "Пошаговая инструкция"
```

Агент сам пройдёт все шаги и сгенерирует документацию.

---

## Подготовка (один раз)

Загрузи файлы в контекст:

```
ЗАГРУЗИ В КОНТЕКСТ:
1. templ.md - шаблон документации
2. KsAPI/ - все заголовочные файлы (включая KsAPI.h ~30K строк)
3. Source/ - примеры использования
```

---

## Пошаговая инструкция

### Шаг 1: Подготовка базы знаний

**Команда:**

```
ЗАГРУЗИ В КОНТЕКСТ СЛЕДУЮЩИЕ ФАЙЛЫ:
1. templ.md
2. Все файлы из папки KsAPI/
3. Все файлы из папки Source/

ПОДТВЕРДИ ЧТО ВСЕ ФАЙЛЫ ДОСТУПНЫ.
```

**Важно:** При документировании интерфейсов приложения всегда упоминай обязательные функции библиотеки:

```cpp
APP_EXP_FUNC(bool) LoadKompasLibrary(ksapi::IApplication & app,
                                     ksapi::IKompasLibraryActions & libaryActions)
{
    // app - экземпляр приложения КОМПАС
    return true;
}
APP_EXP_FUNC(void) UnloadKompasLibrary() { }
```

### Шаг 2: Найти определение интерфейса

**Команда:**

```
НАЙДИ В KsAPI.h ОПРЕДЕЛЕНИЕ ИНТЕРФЕЙСА: [ИмяИнтерфейса]

ИЩИ:
struct [ИмяИнтерфейса] : [Родитель]

НАЙДИ ВСЕ виртуальные методы внутри struct
```

**Формат вывода:**

```cpp
struct IИмяИнтерфейса : IРодитель
{
    virtual тип Метод(параметры) = 0;
    // ... все методы
};
```

### Шаг 3: Найти способы получения

**Команда:**

```
НАЙДИ В KsAPI.h ВСЕ СПОСОБЫ ПОЛУЧЕНИЯ ИНТЕРФЕЙСА: [ИмяИнтерфейса]

ИЩИ:
- Get[Интерфейс]()
- Add[Интерфейс]()
- Create[Интерфейс]()
- QueryInterface для IID_[Интерфейс]
```

### Шаг 4: Найти дополнительные интерфейсы

**Команда:**

```
НАЙДИ ДОПОЛНИТЕЛЬНЫЕ ИНТЕРФЕЙСЫ ДЛЯ: [ИмяИнтерфейса]

ИЩИ:
В файле side_interfaces.json (основной источник)
```

### Шаг 5: Найти примеры использования

**Команда:**

```
НАЙДИ В Source/ ВСЕ ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ: [ИмяИнтерфейса]

ИЩИ:
- [ИмяИнтерфейса]Ptr
- Вызовы методов интерфейса
```

### Шаг 6: Сгенерировать документацию

**Команда:**

```
СОЗДАЙ ДОКУМЕНТАЦИЮ ДЛЯ: [ИмяИнтерфейса]

ИСПОЛЬЗУЙ ШАБЛОН: templ.md

ЗАПОЛНИ ВСЕ РАЗДЕЛЫ:
1. Иерархия наследования
2. Общее описание
3. Важные примечания
4. Получение интерфейса
5. Дополнительные интерфейсы
6. Методы (для каждого: кратко, полное описание, синтаксис, параметры, примеры)
7. Частые ошибки
8. Примеры из Source/
9. Шаблоны использования
10. Связанные интерфейсы
```

---

## Правила документирования

### Методы

- **Кратко** - 1-2 предложения
- **Полное описание** - подробнее
- **Синтаксис** - точная сигнатура
- **Параметры** - с in/out
- **Примеры** - из Source/
- **Примечания** - особенности

### Примеры кода

Правильно:

```cpp
ksapi::I[Интерфейс]Ptr obj = // получение интерфейса I[Интерфейс]
obj->Метод();
```

Неправильно:

```cpp
ksapi::I[Интерфейс]Ptr obj = nullptr;  // не надо так
```

### Enum

- ≤7 констант → включить в описание метода
- \> 7 констант → ссылка на файл в `files/`

### Ссылки

- Всегда добавляй ссылки на интерфейсы (даже если страницы нет)
- НЕ используй номера строк в ссылках

### Язык

- Только русский и английский
- Без китайского, польского и др.

---

## Чеклист проверки

- [ ] Все методы задокументированы
- [ ] Структура по шаблону
- [ ] Есть примеры для сложных методов
- [ ] Указаны способы получения
- [ ] Перечислены дополнительные интерфейсы
- [ ] Нет опечаток
- [ ] Нет номеров строк в ссылках

---

## Важно: Обновляй инструкцию

Если при генерации документации ты находишь ошибки или неточности, которые относятся к:

- Структуре документации
- Формату определённых блоков текста
- Примерам кода
- Правилам документирования

То есть не к конкретному интерфейсу, а к общему процессу - **вноси изменения в эту инструкцию и в шаблон templ.md**.

Это поможет избежать подобных ошибок в будущем при документировании других интерфейсов.

---

## Пример для конкретного интерфейса

```
СОЗДАЙ ДОКУМЕНТАЦИЮ ДЛЯ ИНТЕРФЕЙСА IApplication

ОСОБОЕ ВНИМАНИЕ:
1. Это корневой интерфейс - обязательно укажи функции LoadKompasLibrary
2. Много методов для работы с документами
3. Примеры из Source/Cube/, Source/Events/
```
